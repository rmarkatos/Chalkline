# scratch pages the test suites write beside themselves
_*.html
# screenshots the suites take
shot-*.png
node_modules/

# macOS clutter
.DS_Store

# local settings — holds the teacher email, must never be published
chalkline-local.json
# the ready-to-run SQL that build.py fills in from it
supabase-schema.local.sql

# scratch page test-boot.js writes beside itself
_boot.html
